# This directory is where results files (CSV, SVG, HDF5, ZIP) from running the tools are stored. This file
# exists so the directory is not empty and git will allow it to be commit to the repository. Without this file,
# the directory would be created when the user runs the tools